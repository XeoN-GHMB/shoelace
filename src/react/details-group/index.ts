import * as React from 'react';
import { createComponent } from '@lit-labs/react';
import Component from '../../components/details-group/details-group.js';

export default createComponent(React, 'sl-details-group', Component, {});
