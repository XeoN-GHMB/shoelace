import * as React from 'react';
import { createComponent } from '@lit-labs/react';
import Component from '../../components/row/row.js';

export default createComponent({
  tagName: 'sl-row',
  elementClass: Component,
  react: React,
  events: {}
});
