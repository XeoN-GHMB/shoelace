import * as React from 'react';
import { createComponent } from '@lit-labs/react';
import Component from '../../components/badge/badge.js';

export default createComponent(React, 'sl-badge', Component, {});
