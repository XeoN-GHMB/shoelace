import * as React from 'react';
import { createComponent } from '@lit-labs/react';
import Component from '../../components/layout/layout.js';

export default createComponent(React, 'sl-layout', Component, {});
