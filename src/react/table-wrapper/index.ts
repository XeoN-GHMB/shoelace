import * as React from 'react';
import { createComponent } from '@lit-labs/react';
import Component from '../../components/table-wrapper/table-wrapper.js';

export default createComponent(React, 'sl-table-wrapper', Component, {});
